projection implementation in class ZCL_C_CI_H unique;
strict ( 2 );
use draft;
define behavior for ZC_TB_CI_H alias CustomerInvoiceHeader
use etag

{
  //  use create;
  //  use update;
  //  use delete;

  action print result [1] $self;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}